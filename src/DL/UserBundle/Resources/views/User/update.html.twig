{% extends 'DLFamilytreeBundle::base.html.twig' %}
{% block stylesheets %}
<link rel="stylesheet" href="{{ asset('bundles/dluser/css/update.css') }}" />

{% endblock %}
{% block body %}


<div class="container" style="background-image:url({{ asset('images/view/arbres.jpg') }})">

    <div class="content">
        <h1>Formulaire de modification de l'utilisateur : {{ user.username }}</h1>


        {{ form_start(form) }}

        {{ form_end(form) }}

        {% if  app.session.flashbag.get('notice') is defined %}
        {% for flashMessage in app.session.flashbag.get('notice') %}

        <div class="alert alert-success" style="color : red">
            {{ flashMessage }}
        </div>

        {% endfor %}
        {% endif %}
        {{ form_start(form2) }}

        {{ form_end(form2) }}

    </div>
</div>


{% endblock %}
